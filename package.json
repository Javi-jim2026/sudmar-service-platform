{
  "name": "sudmar-service-platform",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Dashboard operativo de SUDMAR basado en el Excel vigente",
  "scripts": {
    "start": "python3 -m http.server 8080 --bind 127.0.0.1 --directory prototype",
    "test": "node --test tests/core.test.mjs"
  }
}
